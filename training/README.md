# External training notebooks

Keep notebooks thin. Use them for data inspection, export validation, and external orchestration. Import versioned repository code rather than duplicating preprocessing, architecture, baselines, or evaluation logic.

The first milestone compares seasonal-naive, moving-average, XGBoost, and compact GRU or LSTM candidates on identical chronological periods.
